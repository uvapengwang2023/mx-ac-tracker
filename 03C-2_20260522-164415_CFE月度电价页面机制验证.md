# 03C-2｜CFE 月度电价页面机制验证

- 创建时间：2026-05-22 16:44:15（Asia/Shanghai）
- 所属任务：03C-2 第一步
- 任务边界：只验证 CFE 月度电价页面的数据获取机制，不做全量抓取，不建立正式电价数据库。
- 原则：不杜撰数据；不能稳定取得的内容标记为“未验证/需复核”。

---

## 1. 本步骤结论

本轮验证的结论是：CFE 家用电价页面是官方来源，但不能简单用 `curl` 或静态 HTML 抓取方式直接批量获取月度价格。

更准确地说：

1. CFE 官方页面的初始内容可以被部分工具读取，能确认 tariff 名称、适用说明、月份选择项、DAC 门槛等规则文本。
2. 具体月度价格通常需要触发 ASP.NET WebForms 的 postback，也就是选择年份、夏季开始月份、查询月份后，由同一个 `.aspx` 页面返回表格。
3. 在本地 headless browser / CLI 环境下，CFE 存在 Incapsula/Imperva 类访问保护，表现为 `307`、`302 -> 403`、空正文、postback 后页面变空等情况。
4. 小样本里 `Tarifa1F.aspx` 的 postback 曾成功返回价格表，但 `Tarifa1.aspx`、`Tarifa1C.aspx`、`TarifaDAC.aspx` 在同一轮测试中出现不稳定或空页面，因此动态页面 postback 暂不能作为唯一稳定抓数路径。
5. 发现 CFE 还有 `Acervo histórico tarifario` 官方入口，搜索索引显示其可能包含按月展开的家用电价表；但本地 headless browser 直接访问该入口返回 `403`，暂只作为候选路径，不能直接作为已验证数据源。

因此，后续 03C-2 不应直接进入全量抓取。建议下一步先做“小样本稳定性验证”：同一 tariff、同一月份，分别用浏览器 postback、官方页面文本、可能的 Acervo 页面三条路径交叉验证。

---

## 2. 已验证的官方页面入口

本轮检查的 CFE 官方入口如下：

| 页面 | 用途 | 初始页面是否能读到规则文本 | 备注 |
| --- | --- | --- | --- |
| `Tarifa1.aspx` | 普通家用 Tarifa 1 | 是 | 初始页可见年份和月份选择；postback 不稳定 |
| `Tarifa1C.aspx` | Tarifa 1C，夏季最低平均温度 30°C 地区 | 是 | 初始页可见“夏季开始月份”和“查询月份”两个选择框；postback 不稳定 |
| `Tarifa1F.aspx` | Tarifa 1F，夏季最低平均温度 33°C 地区 | 是 | 小样本 postback 成功返回过价格表 |
| `TarifaDAC.aspx` | Doméstica de Alto Consumo | 是 | 初始页可读 DAC 规则；postback 不稳定 |
| `AcervoHistorico/Tarifas/CuotasMenAutDom` | 历史家用电价表候选入口 | 搜索索引显示有表格，但本地访问未通过 | 需后续单独验证，不可直接用搜索摘要代替数据 |
| `AcervoHistorico/Tarifas/CuotasMenAutDac` | 历史 DAC 电价表候选入口 | 搜索索引显示有表格，但本地访问未通过 | 需后续单独验证 |

---

## 3. 静态请求测试结果

静态请求包括 `curl`、`HEAD`、普通 GET。

观察结果：

| 测试方式 | 结果 | 含义 |
| --- | --- | --- |
| 普通 `curl` GET | 多次出现超时、空正文或无法稳定返回内容 | 不能作为主抓取方式 |
| `HEAD` 请求 | 观察到 `HTTP/2 307`、`set-cookie`、`x-iinfo` 等访问保护相关响应 | CFE 页面存在访问保护，简单脚本请求风险高 |
| 本地 headless browser 访问 Acervo 页面 | 出现 `302 -> 403`，并返回 Incapsula incident 页面 | Acervo 路径虽有价值，但本地环境尚未验证可抓 |

判断：不建议用纯 `curl` 直接抓 CFE 月度电价。若后续需要自动化，至少需要浏览器会话、cookies、重试与失败标记。

---

## 4. 页面表单机制

CFE tariff 页面表现为 ASP.NET WebForms 页面，月份选择通过 `__doPostBack(...)` 回传同一个 `.aspx` 页面。

### 4.1 Tarifa 1

观察到的关键控件：

| 控件 | DOM id | name | 说明 |
| --- | --- | --- | --- |
| 年份 | `ContentPlaceHolder1_Fecha_ddAnio` | `ctl00$ContentPlaceHolder1$Fecha$ddAnio` | 可选年份观察到 2026 至 2017 |
| 查询月份 | `ContentPlaceHolder1_MesVerano1_ddMesConsulta` | `ctl00$ContentPlaceHolder1$MesVerano1$ddMesConsulta` | 月份 value 为 1-12，另有 `0=---Selecciona---` |

### 4.2 Tarifa 1C / 1F

观察到的关键控件：

| 控件 | DOM id | name | 说明 |
| --- | --- | --- | --- |
| 年份 | `ContentPlaceHolder1_Fecha_ddAnio` | `ctl00$ContentPlaceHolder1$Fecha$ddAnio` | 可选年份观察到 2026 至 2017 |
| 夏季开始月份 | `ContentPlaceHolder1_MesVerano1_ddMesVerano` | `ctl00$ContentPlaceHolder1$MesVerano1$ddMesVerano` | 可选 `FEBRERO`、`MARZO`、`ABRIL`、`MAYO` |
| 查询月份 | `ContentPlaceHolder1_MesVerano2_ddMesConsulta` | `ctl00$ContentPlaceHolder1$MesVerano2$ddMesConsulta` | 月份 value 为 1-12，另有 `0=---Selecciona---` |

### 4.3 Tarifa DAC

观察到的关键控件：

| 控件 | DOM id | name | 说明 |
| --- | --- | --- | --- |
| 年份 | `ContentPlaceHolder1_Fecha_ddAnio` | `ctl00$ContentPlaceHolder1$Fecha$ddAnio` | 工具间观察到年份展示不一致，需要复核 |
| 查询月份 | `ContentPlaceHolder1_Fecha1_ddMes` | `ctl00$ContentPlaceHolder1$Fecha1$ddMes` | 本地 browser 观察到部分月份选项，但 postback 不稳定 |

注意：不同工具访问同一页面时，Tarifa DAC 的年份展示存在不一致现象；后续必须以同一访问方式、同一时间重新复核，不能混用观察结果。

---

## 5. 小样本 postback 测试

本轮只做机制验证，不把以下结果纳入正式价格数据库。

| 测试项 | 操作 | 结果 | 是否可作为正式数据 |
| --- | --- | --- | --- |
| Tarifa 1，2026，MAYO | 选择查询月份 `MAYO` | 触发 `POST document`，但最终页面正文为空 | 否 |
| Tarifa 1C，2026，夏季开始 `MAYO` | 选择夏季开始月份 `MAYO` | 触发 `POST document`，后续查询月份控件消失/无法继续 | 否 |
| Tarifa 1F，2026，夏季开始 `MAYO`，查询月份 `MAYO` | 依次选择夏季开始月份与查询月份 | 成功返回价格表 | 否，本轮仅证明机制可行 |
| Tarifa DAC，MAYO | 选择查询月份 `MAYO` | 触发 `POST document`，但最终页面正文为空 | 否 |

Tarifa 1F 小样本返回的表格文本为：

| 档位 | 样本返回值 | 说明 |
| --- | --- | --- |
| Consumo básico | 0.839 | por cada uno de los primeros 300 kWh |
| Consumo intermedio bajo | 1.039 | por cada uno de los siguientes 900 kWh |
| Consumo intermedio alto | 2.526 | por cada uno de los siguientes 1300 kWh |
| Consumo excedente | 3.992 | por cada kWh adicional |

这组值只能作为“postback 成功返回表格”的样本证据，后续需要在同一 tariff、同一月份下复核来源路径后，才可进入正式电价表。

---

## 6. 主要风险

| 风险 | 表现 | 对后续任务的影响 |
| --- | --- | --- |
| 访问保护 | `307`、`302 -> 403`、Incapsula incident、空正文 | 自动化抓取可能失败或不稳定 |
| WebForms postback | 依赖页面状态、表单字段、session/cookies | 不能只拼 URL 或普通 GET |
| 页面状态不一致 | 同一页面在不同访问方式下显示年份或月份范围不一致 | 必须统一访问方法并保存运行日志 |
| 夏季开始月份 | 1A-1F 需要先选择夏季开始月份，再选择查询月份 | 后续电价表必须保留 `summer_start_month` 字段 |
| 搜索索引摘要 | 搜索结果能看到 Acervo 表格片段，但本地访问未通过 | 不能用搜索摘要替代原始数据 |
| 价格字段口径 | CFE 表格是分档价格，不是单一电价 | 后续制冷成本模型要按用电量分段计算 |

---

## 7. 后续建议的第二步

建议 03C-2 第二步不要全量抓取，而是做“小样本稳定性验证”：

1. 选 2 个 tariff：`Tarifa 1F` 和 `Tarifa 1C`。
2. 选 2 个月份：一个夏季月份、一个非夏季月份。
3. 对每个样本保存：
   - tariff；
   - 年份；
   - 夏季开始月份；
   - 查询月份；
   - 页面访问方式；
   - 是否成功返回表格；
   - 返回表格原文；
   - 解析后的结构化字段；
   - 失败时的 HTTP 状态、错误页面或空正文标记。
4. 若 Acervo 页面能稳定访问，则优先评估 Acervo 是否可以替代动态 postback 作为主数据源。
5. 只有当小样本可复现，才进入全量 tariff/month 抓取。

---

## 8. 对原任务台账的影响

03C 第一版已经完成了规则口径；03C-2 当前只完成“页面机制验证”的第一步。

后续任务顺序建议调整为：

1. 先完成 03C-2 第二步：小样本稳定性验证。
2. 若稳定，再做 03C-2 第三步：正式月度电价表。
3. 若不稳定，转向 03E 热负荷或 03D 收入数据，暂缓月度电价抓取，只保留 03C 已确认的 tariff 规则和 DAC 门槛。

