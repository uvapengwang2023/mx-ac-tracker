# A02 参数抽取口径修正：冷媒、尺寸与 OCR 方法

记录时间：2026-06-06

## 背景

在 Prime 空调参数整理复核中发现三个问题：

1. 冷媒充注量的源文件单位不统一，部分为 `g`，部分为 `kg`，当前结果没有系统性统一到 `kg`。
2. 参数字段有缺漏，尤其是室内机/室外机的裸机尺寸和包装箱尺寸没有同时完整输出。
3. 部分产品手册会把单冷机型和冷暖机型的尺寸拆成两行，当前整理容易把这些差异合并或遗漏。
4. OCR 错误较多，说明不能把普通 OCR 作为参数表抽取主流程。

## 结论

A02 后续不能只做“标题字段抽取”，还要补一条“产品参数表抽取”子流程。

当前 Prime 参数整理结果可以作为人工整理样本，但不能作为最终自动化口径。后续必须升级为：

```text
PDF/图片来源 -> 文本层/坐标表格抽取 -> 字段标准化 -> 单位转换 -> 异常校验 -> 人工复核
```

## 冷媒量口径

冷媒类型和冷媒充注量必须拆开。

必须保留：

- `refrigerant_raw`：原文，例如 `R410a/2010g`、`R32/500g`、`R32`。
- `refrigerant_type`：规范冷媒类型，例如 `R410A`、`R32`。
- `refrigerant_charge_raw`：原始充注量文本，例如 `2010g`、`0.65 kg`。
- `refrigerant_charge_unit_raw`：源单位，例如 `g`、`kg`。
- `refrigerant_charge_kg`：统一 kg 后的数值。
- `refrigerant_charge_source`：来源页/来源单元格/来源截图。

转换规则：

| 源单位 | 处理 |
|---|---|
| `g` / `gramos` | 除以 1000，写入 kg |
| `kg` / `Kg` / `KGS` | 直接写入 kg |
| 表头单位为 `g`，单元格只有数字 | 按表头单位转换 |
| 原文形如 `R32/500g` | 拆成 `R32` 和 `0.5 kg` |
| 只有冷媒类型，没有充注量 | `refrigerant_charge_kg` 留空，并标记 `refrigerant_charge_missing` |

禁止：

- 不允许把 `500g`、`0.5kg`、`R32` 混放在同一个规范字段里。
- 不允许因为源文件单位不同就在最终表里保留多个单位。
- 不允许只保留冷媒类型而丢失充注量。

## 尺寸字段口径

室内机和室外机要分开，裸机尺寸和包装尺寸也要分开。

建议字段：

| 字段 | 含义 |
|---|---|
| `indoor_unit_dimensions_raw` | 室内机裸机尺寸原文 |
| `indoor_unit_width_mm` | 室内机裸机宽 |
| `indoor_unit_depth_mm` | 室内机裸机深 |
| `indoor_unit_height_mm` | 室内机裸机高 |
| `indoor_package_dimensions_raw` | 室内机包装尺寸原文 |
| `indoor_package_width_mm` | 室内机包装宽 |
| `indoor_package_depth_mm` | 室内机包装深 |
| `indoor_package_height_mm` | 室内机包装高 |
| `outdoor_unit_dimensions_raw` | 室外机裸机尺寸原文 |
| `outdoor_unit_width_mm` | 室外机裸机宽 |
| `outdoor_unit_depth_mm` | 室外机裸机深 |
| `outdoor_unit_height_mm` | 室外机裸机高 |
| `outdoor_package_dimensions_raw` | 室外机包装尺寸原文 |
| `outdoor_package_width_mm` | 室外机包装宽 |
| `outdoor_package_depth_mm` | 室外机包装深 |
| `outdoor_package_height_mm` | 室外机包装高 |
| `dimension_order_raw` | 原表顺序，例如 `ancho x prof x alto`、`ancho x alto x fondo` |

注意：

- 西语表格里的 `ancho x prof x alto` 和 `ancho x alto x fondo` 顺序不同，不能只按三个数字硬拆。
- `profundidad` / `fondo` 都可表示深度，但必须保留原始顺序说明。
- 最终展示可以继续用 `宽×深×高 mm`，但底层字段必须有拆分数值和原始文本。

## 单冷/冷暖分行口径

有些产品手册在写产品尺寸时，会把单冷机型和冷暖机型分成两行。

后续不能只按容量合并，必须加 `mode_variant`：

| 字段 | 示例 |
|---|---|
| `mode_variant` | `solo_frio` / `frio_calor` / `common` |
| `indoor_model_code` | `EMPRC122-E4` / `EMPRN122-E4` |
| `outdoor_model_code` | `CMPRC122-E4` / `CMPRN122-E4` |
| `dimension_scope` | `indoor_unit` / `indoor_package` / `outdoor_unit` / `outdoor_package` |

规则：

- 如果源表明确分单冷和冷暖，必须输出两条记录或一个子表，不允许合并成一条。
- 如果源表没有区分，`mode_variant = common`。
- 型号前缀要参与判断：`EMPRC/CMPRC` 通常对应单冷，`EMPRN/CMPRN` 通常对应冷暖。
- A03 以后横向对比时，单冷和冷暖不能混在一起比。

## OCR 方法修正

普通 OCR 不适合作为空调参数表的主流程。

原因：

- 参数表是多列表格，OCR 容易读对字但读错行列。
- 冷媒、尺寸、重量、电流、电压都是短数字，OCR 一旦错位，很难通过语义自动发现。
- `0.65 kg`、`0.45 kg`、`500g`、`5.00` 这类值很容易被误读或错列。
- PDF 文本层存在时，普通 OCR 会丢掉本来可用的文本和坐标信息。

更好的方案：

1. PDF 优先使用文本层和坐标抽取。
   - 使用 `pdfplumber` 或 `PyMuPDF` 提取每个词的 `x/y` 坐标。
   - 通过表格线、行列位置、字段标签锚点恢复表格。
   - `pypdf` 只能证明文本层存在，但线性文本顺序会把表格打散，不适合作为最终表格解析工具。
2. 对扫描版 PDF 或纯图片，再用高分辨率渲染和视觉模型。
   - 不做整页 OCR 直接入表。
   - 先定位表格区域，再按行/列切块识别。
   - 每个关键单元格保留截图证据和置信度。
3. OCR 只作为兜底。
   - 只在没有文本层、没有可用坐标或图片源时使用。
   - OCR 结果必须进入低置信度复核清单。
4. 关键字段必须有校验。
   - 冷媒量单位转换校验。
   - 尺寸字段数量和单位校验。
   - 裸机尺寸应小于包装尺寸。
   - 室内机尺寸和室外机尺寸不能互换。
   - 单冷/冷暖分行不能被合并。

## 后续执行建议

下一步不要继续在旧 Excel 上手工补字段，而是先升级抽取工具链。

建议顺序：

1. 建立参数字段 schema，尤其补齐冷媒充注量、裸机/包装尺寸、单冷/冷暖维度。
2. 用 `pdfplumber` 或 `PyMuPDF` 做 PDF 坐标化抽取原型。
3. 对 Prime 已有 PDF 做 3 份样本测试：`ELITE_4_POPUP.pdf`、`TRENDY_3_POPUP.pdf`、`FICHA_V3_compressed.pdf`。
4. 输出“原始表格候选 + 规范字段 + 异常清单”。
5. 人工复核后再重新生成 Prime 参数汇总表。

## 当前状态判断

这个问题属于 A02 的前置能力，不是 A03 问题。只有当产品系列和关键参数字段都稳定后，A03 的竞品池自动分层才有可靠基础。
