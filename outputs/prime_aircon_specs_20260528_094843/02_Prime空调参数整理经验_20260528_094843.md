# Prime 空调参数整理经验记录

时间戳：20260528_094843  
范围：Elite 2、Elite 4、Trendy K、Trendy 3  
版本说明：根据反馈修正功能卖点覆盖范围，并保存链接中的产品录入/上传年月。

## 本次修正输出

- 新建修正版汇总工作簿：`01_Prime空调参数汇总_20260528_094843.xlsx`
- 同步导出 CSV：`01_Prime空调参数汇总_20260528_094843.csv`
- 未覆盖前一版：`outputs/prime_aircon_specs_20260528_092734/`

## 修正点

1. `功能卖点` 表已改为按产品/系列展开，覆盖 4 个产品：Elite 2、Elite 4、Trendy K、Trendy 3。
2. 新增 `网站录入年月_链接` 字段，用于保存 URL 中 `/uploads/YYYY/MM/` 暗含的网站录入或上传年月。
3. `来源与口径` 表也新增同名字段，方便审计来源 URL 的时间信息。

## 链接时间口径

- Elite 2 图片 URL：`/uploads/2023/10/`，记录为 `2023-10`。
- Elite 4 PDF URL：`/uploads/2025/06/`，记录为 `2025-06`。
- Trendy K PDF URL：`/uploads/2025/06/`，记录为 `2025-06`。
- Trendy 3 PDF URL：`/uploads/2025/06/`，记录为 `2025-06`。
- 不含 `/uploads/YYYY/MM/` 的链接不强行推断时间，字段留空或沿用主来源的录入年月。

## 经验更新

1. 功能卖点不能只看本轮新增文件；做汇总时要回看历史批次，确保旧产品也进入同一张功能表。
2. 链接中的上传年月是重要商品信息，应作为结构化字段进入核心表、扩展表和来源表。
3. 时间字段应记录为 `YYYY-MM`，不要把它混在 URL 文本里，否则后续筛选和按批次分析会不方便。
4. 对不含时间片段的补充链接，只保留为来源，不用猜测上传时间。

## 仍需复核的源文件异常

- `TRENDY_K_POPUP.pdf`：`362-K` 的 `SEER` 显示为 `2.2`，低压显示 `50 MPa`，室内低档噪音显示 `58 dB(A)`；已按源文件保留。
- `TRENDY_3_POPUP.pdf`：12K 列表头为 `122-T3`，但型号格显示 `EMPRC121-T3 / EMPRN121-T3 / CMPRC121-T3 / CMPRN121-T3`；已按源文件保留并标注。
- `ELITE_4_POPUP.pdf`：`122-E4` 冷媒充注量已用渲染页核对为单冷 `0.45 kg`、冷暖 `0.65 kg`。
