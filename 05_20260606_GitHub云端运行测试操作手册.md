# GitHub 云端运行测试操作手册

这份手册用于测试：GitHub 云端是否可以自动运行爬虫，以及是否必须配置美国/墨西哥代理。

## 先理解三件事

1. GitHub Actions 是 GitHub 提供的临时云电脑。到点后，它会自动开机，下载项目，运行脚本，然后关机。
2. 你的笔记本电脑不需要开着；本机 VPN 也不会被 GitHub 使用。
3. 如果电商网站限制 IP，就要在 GitHub Secrets 里配置 `PROXY_URL`，让云端通过美国/墨西哥代理访问。

## 第 1 轮：不配置代理，测试 GitHub 直连

目的：看 GitHub 云端默认 IP 能不能访问部分网站。

建议先只跑：

- Coppel
- Elektra

不要一开始跑 HomeDepotMX，因为 HomeDepotMX 原本就更依赖代理/地区出口。

操作：

1. 打开 GitHub，新建一个仓库。
2. 把本项目推送到这个仓库。
3. 打开仓库的 `Actions` 页面。
4. 选择 `Cloud Daily Crawl`。
5. 点击 `Run workflow`。
6. `sites` 填：

```text
coppel,elektra
```

7. `publish_pages` 选择 `false`。
8. `allow_direct_ip` 保持默认 `true` 也可以；跑 Coppel/Elektra 时这个开关不会影响它们。
9. 点击运行。

判断结果：

- 如果成功：GitHub 云端直连至少可以跑 Coppel/Elektra。
- 如果失败，并且日志里是 403、timeout、access denied、captcha、blocked：需要代理。

## 第 2 轮：测试 HomeDepotMX 直连

目的：判断 HomeDepotMX 是否也可以不用代理。

操作：

1. 再次点击 `Run workflow`。
2. `sites` 填：

```text
homedepotmx
```

3. `allow_direct_ip` 选择 `true`，或保持默认 `true`。
4. `publish_pages` 选择 `false`。

判断结果：

- 如果成功：GitHub 云端 IP 也能直连 HomeDepotMX。
- 如果失败：HomeDepotMX 云端运行需要 `PROXY_URL`。

## 第 3 轮：配置代理后测试

如果第 1 轮或第 2 轮失败，就配置代理。

GitHub 操作路径：

1. 仓库页面进入 `Settings`。
2. 左侧进入 `Secrets and variables`。
3. 点击 `Actions`。
4. 点击 `New repository secret`。
5. Name 填：

```text
PROXY_URL
```

6. Secret 填你的代理地址，例如：

```text
http://username:password@host:port
```

可选再加一个 Secret：

```text
VPN_CHECK_URL
```

值可以填：

```text
https://www.homedepot.com.mx/
```

然后重新运行：

```text
coppel,elektra,homedepotmx
```

## 重要提醒

- GitHub 不能使用你电脑上的 VPN。
- GitHub 云端如果不用代理，就是 GitHub 自己的云服务器 IP。
- WalmartMX 当前需要人工验证，不建议放进云端无人值守任务。
- 第一次云端运行只建立基线；第二次以后才更适合看价格变化。
