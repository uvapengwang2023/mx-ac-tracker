# A03 竞品池自动分层第一版记录

记录时间：2026-06-06

## 阶段结论

A03 第一版已经进入技术落地，但业务使用需要暂缓。

A03 第一版的目标不是直接生成最终竞品名单，而是把 Prime 目标 SKU 周围的可比较商品先自动分层，形成一个可复核的候选池。最终哪些是真竞品、弱竞品、不可比，仍由人工判断。

2026-06-06 口径修正：

- A03 暂时不适合继续推进到正式竞品池。
- 原因是 A02 中的产品系列整理还没有完成，而产品系列通常代表一个品牌内部从低到高的价格阶梯和产品线定位。
- 同一个品牌通常会按系列区分低、中、高价格段，很少在非常相近的价格段内布局多个高度重叠的系列。
- 如果产品系列和系列价格阶梯没有先整理清楚，A03 容易把“规格相近但产品线定位不同”的商品误判成横向竞品。
- 因此，当前 A03 输出只能视为技术原型和候选演示，不能作为正式竞品池使用。

## 后续正式重启 A03 的硬门槛

做竞品池自动分层时，进入横向对比的商品必须先满足硬门槛。价格、品牌、系列、平台只能用于第二层排序和解释，不能覆盖硬门槛。

硬门槛：

- 产品类型相同：例如 minisplit 对 minisplit，不能混入便携空调、窗机、冷风机、风扇或配件。
- 制冷能力相同或明确等价：例如 `1T` 对 `1T`，`12000 BTU` 对 `12000 BTU`。
- 单冷/冷暖相同：`solo_frio` 不和 `frio_calor` 横向对比。
- 电压相同：`110/115/127V` 不和 `220/230V` 横向对比。
- 变频/定频原则上相同：至少不能被价格接近、品牌权重或系列信号覆盖。

产品系列的作用：

- 系列不是普通文本字段，而是产品线定位字段。
- A02 需要先整理品牌系列、型号到系列映射、系列价格阶梯。
- A03 重启后，系列用于判断候选是否处在相近产品线层级；但系列不能替代制冷能力、冷暖、电压等核心规格。

## 已完成内容

新增脚本：

```bash
npm run competitors:build
```

新增输出：

- `data/competitor_candidates/latest_competitor_candidates.json`
- `data/competitor_candidates/latest_competitor_candidates.csv`

新增 SQLite 表：

- `competitor_candidate_runs`
- `competitor_candidates`

新增 dashboard 模块：

- `竞品池候选`
- 展示目标 SKU、候选数量、候选层级、候选品牌、系列、规格、价格、价差、可比原因和风险。

云端 artifact 已加入：

- `data/competitor_candidates/**`

每日云端任务链路已加入：

```text
crawler -> sku:normalize -> competitors:build -> alerts:build -> dashboard:build
```

## 当前结果

基于当前 `data/normalized_skus/latest_normalized_skus.json`：

| 指标 | 数值 |
|---|---:|
| Prime 目标 SKU | 27 |
| 候选关系 | 318 |
| 直接竞品 | 171 |
| 强候选 | 145 |
| 价格/规格参照 | 2 |
| 有直接或强候选的目标 SKU | 27 |
| 平均每个目标候选数 | 11.8 |

## 分层口径

当前技术原型的候选关系打分字段：

- 产品类型：同类型优先，`minisplit` 和 `other_ac` 可作为近似类型。
- 容量：同 Ton 位是直接竞品的必要条件。
- 电压：110/127V 与 220/230V 分开。
- 变频/定频：作为核心可比字段。
- 单冷/冷暖：作为核心可比字段。
- 价格接近度：用于排序和价格带参照，不覆盖核心规格。
- 系列信号：标题明确系列或型号族可以增强解释性。
- 重点竞品品牌：Mirage、Midea、Mabe、LG、Hisense、Samsung、Whirlpool、York。

当前技术原型的层级定义：

- 直接竞品：非 Prime、容量一致、产品类型兼容、关键规格完整且一致。
- 强候选：非 Prime、容量一致、产品类型兼容，部分字段略有缺口或置信度不足。
- 价格/规格参照：可参考价格或规格，但不能直接视为真竞品。
- 内部参照：Prime 同品牌跨平台或同规格参考，不算外部竞品。
- 观察项：弱候选，暂不进入核心竞品池。

## 重要经验

1. 竞品池要先做候选，不要一步到位写成最终名单。
2. 缺电压、缺冷暖、缺变频信息时，不能强行归为直接竞品。
3. 容量一致是必要条件，但容量一致本身不代表可比。
4. 产品系列是解释字段，不应压过容量、电压、冷暖、变频等核心规格。
5. 型号和系列名之间有联系，可以用于提高候选解释性，但低置信度型号族必须保留人工复核标记。
6. 价格接近有助于排序，但不能用低价或高价单独判断竞品关系。
7. Dashboard 应展示“原因/风险”，让人工复核更快，而不是只展示一个黑箱分数。

## 人工复核建议

原计划是做 A03 抽样复核，但根据口径修正，下一步应先回到 A02 产品系列整理。

优先动作：

- 整理 Prime、Mirage、LG、Midea、Mabe 的产品系列。
- 明确标题显式系列、型号特征推断系列、营销功能词之间的边界。
- 整理同品牌系列的价格阶梯。
- 确认哪些型号前缀/后缀可以推断系列。
- 等 A02 系列整理完成后，再重启 A03 并按硬门槛重写分层规则。

## 当前判断

A03 第一版已经能支持“从标题字段到竞品候选”的技术闭环：

```text
商品标题/价格 -> SKU 标准字段 -> Prime 目标池 -> 候选竞品分层 -> dashboard 查看 -> 人工复核
```

但当前业务主线应回到 A02。只有当产品系列和核心规格硬门槛都整理清楚后，A03 才适合进入正式竞品池自动分层。
