# GitHub 云端三站点验证复盘

记录时间：2026-06-06

本次目标：验证 GitHub Actions 是否可以在云端自动运行 Coppel、Elektra、HomeDepotMX 三个墨西哥电商站点爬虫，并生成可下载的 dashboard 数据。

## 结论

GitHub 云端方案可以跑通当前三站点任务。

本次不配置 `PROXY_URL`，使用 GitHub 云端直连 IP，三个站点预检均返回 `200 OK`，三站点爬虫均成功，dashboard 数据也成功生成并上传为 artifact。

这说明目前阶段不必立刻购买或配置云端代理。代理仍然要保留为备用方案：一旦未来日志出现 `403`、`timeout`、`access denied`、验证码或明显封禁，再配置 `PROXY_URL`。

## 本次验证结果

- GitHub Actions run：https://github.com/uvapengwang2023/mx-ac-tracker/actions/runs/27064477121
- Artifact：https://github.com/uvapengwang2023/mx-ac-tracker/actions/runs/27064477121/artifacts/7455102690
- Artifact 名称：`mx-ac-crawl-27064477121`
- Artifact ID：`7455102690`
- Artifact 创建时间：2026-06-06T14:18:29Z
- Artifact 保留时间：30 天
- 云端运行状态：`success`
- Dashboard 生成状态：`success`

站点结果：

| 站点 | 云端预检 | 爬虫状态 | 商品数 |
| --- | --- | --- | ---: |
| Coppel | 200 OK | success | 434 |
| Elektra | 200 OK | success | 220 |
| HomeDepotMX | 200 OK | success | 138 |

Dashboard 汇总：

- 总商品数：792
- 中位价格：MXN 8,309
- 平均价格：MXN 9,748
- 品牌识别覆盖率：82.4%
- 匹数/容量识别覆盖率：84.8%
- 时间范围：2026-06-06 至 2026-06-06

## 验证过的 artifact 内容

下载 artifact 后，确认包含：

- `data/coppel/latest_products.json`
- `data/elektra/latest_products.json`
- `data/homedepotmx/latest_products.json`
- `data/daily_crawls/latest_run.json`
- `data/daily_crawls/logs/`
- `web/competitor-intel-dashboard/index.html`
- `web/competitor-intel-dashboard/styles.css`
- `web/competitor-intel-dashboard/app.js`
- `web/competitor-intel-dashboard/data.js`

`data.js` 可以正常解析，大小约 587KB，不是空文件。网页文件也完整，`index.html` 正确引用了 `styles.css`、`app.js` 和 `data.js`。

## 学到的经验

1. GitHub Actions 可以理解为一台“临时云电脑”。它会自动下载仓库、安装依赖、运行脚本、上传结果，然后关机。
2. 运行 GitHub Actions 不需要本机电脑开着，也不会使用本机 VPN。
3. 云端数据不会天然永久保留，需要通过 artifact、cache 或未来数据库来保存。
4. Artifact 适合保存每次运行的结果包，方便下载查看，但默认只保留 30 天。
5. GitHub Actions cache 可以在多次运行之间保留 `data/`，因此第二次及以后才更适合做价格变化、新品、下架判断。
6. 当前 Coppel、Elektra、HomeDepotMX 可以直连跑通，但这不是永久保证。电商网站的反爬策略可能随时变化。
7. HomeDepotMX 脚本原本设计为更谨慎的站点，云端直连时必须显式允许 `allow_direct_ip=true`。
8. WalmartMX 当前仍不适合加入无人值守云端任务，因为它可能需要人工验证。

## 当前推荐策略

短期继续使用 GitHub Actions 作为云端 MVP：

- 每天自动跑 `coppel,elektra,homedepotmx`
- 暂不配置代理
- 每次运行上传 artifact
- 保留 `data/` cache 用于变化检测
- WalmartMX 继续人工或单独监督运行

触发代理配置的条件：

- 任一站点预检失败
- 日志出现 `403`
- 日志出现 `timeout`
- 日志出现 `access denied`
- 日志出现验证码或手动验证
- 商品数突然变为 0 且不是网站真实下架

## 下一步

1. 等待一次定时任务自动运行，确认不是只能手动触发。
2. 连续观察 2-3 天 artifact，确认 cache 能稳定支撑价格变化判断。
3. 需要对外查看 dashboard 时，再开启 GitHub Pages 发布。
4. 如果要长期保存历史数据，把 GitHub cache 升级为 Supabase、Postgres、S3/R2、BigQuery 或 VPS 磁盘。
5. 未来如果直连失败，再配置 `PROXY_URL`，不需要重写 workflow。

