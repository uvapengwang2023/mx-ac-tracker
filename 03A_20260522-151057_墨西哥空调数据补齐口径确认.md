# 03A｜墨西哥空调数据补齐口径确认

- 创建时间：2026-05-22 15:10:57（Asia/Shanghai）
- 所属台账：`03_20260522-144408_墨西哥空调数据补齐子任务台账.md`
- 子任务目标：确认后续计算墨西哥空调拥有率、区域需求和相关底层数据时使用的数据口径、字段、公式和限制。
- 本文件性质：口径确认文件，不输出正式市场结论。
- 数据原则：不杜撰；未确认的内容不写成事实；代理口径必须标注。

---

## 1. 本次核验结论

本次 03A 已确认以下事项：

| 项目 | 结论 |
| --- | --- |
| 核心来源 | INEGI Censo de Población y Vivienda 2020，Cuestionario Ampliado |
| 核心表 | `Viviendas_CA` |
| 核心变量 | `AIRE_ACON` |
| 权重变量 | `FACTOR` |
| 地理字段 | `ENT`、`MUN`、`LOC50K` |
| 住房标识字段 | `ID_VIV` |
| 初始计算粒度 | 州级；municipio 级放到后续扩展 |
| 初始主口径 | 使用 `FACTOR` 加权；主分母使用加权总住房 |
| 敏感口径 | 同时保留排除 `No especificado` 的有效分母口径 |
| 不能回答的问题 | 每户空调台数、容量段、inverter/fixed-speed、使用频率、品牌、购买时间、替换需求 |

---

## 2. 官方来源与用途

| 来源 | 机构 | 用途 | 链接 |
| --- | --- | --- | --- |
| Censo de Población y Vivienda 2020 - study description | INEGI RNM | 确认项目、覆盖范围、问卷类型、扩展问卷地理覆盖 | https://www.inegi.org.mx/rnm/index.php/catalog/632/study-description |
| `Viviendas_CA` data dictionary | INEGI RNM | 确认表名、表内容、采集工具和字段列表 | https://www.inegi.org.mx/rnm/index.php/catalog/632/data-dictionary/F14?file_name=Viviendas_CA |
| `AIRE_ACON` variable metadata | INEGI RNM | 确认变量定义、问题文本、编码、宇宙 | https://www.inegi.org.mx/rnm/index.php/metadata/export_variable/632/V423/json |
| `FACTOR` variable metadata | INEGI RNM | 确认扩展权重定义 | https://www.inegi.org.mx/rnm/index.php/metadata/export_variable/632/V400/json |
| Microdatos por entidad federativa | INEGI | 后续计算州级空调拥有率 | 见本文第 8 节 |

---

## 3. 项目与表口径

### 3.1 项目口径

使用的统计项目为：

```text
Censo de Población y Vivienda 2020
```

INEGI RNM 元数据中该项目的识别号为：

```text
MEX-INEGI.ESD2.01-CPV-2020
```

本次用于空调拥有率计算的不是基础问卷的 `Viviendas` 表，而是扩展问卷的：

```text
Viviendas_CA
```

原因：

- `Viviendas_CA` 的内容为住宅特征。
- 采集工具为 `Cuestionario Ampliado`。
- `AIRE_ACON` 存在于 `Viviendas_CA` 表。

### 3.2 地理口径

后续计算分两级：

| 阶段 | 粒度 | 说明 |
| --- | --- | --- |
| 第一阶段 | 州级 / entidad federativa | 用于 03B 的重点热区州试算 |
| 第二阶段 | municipio / demarcación territorial | 处理量更大，且需要更谨慎看样本精度，暂不在 03A 计算 |

注意：

- `ENT` 是州/实体代码。
- `MUN` 是 municipio 或 demarcación territorial 代码。
- `LOC50K` 是 50,000 人及以上地方的 locality 代码。
- 地理代码必须按字符串处理，避免丢失前导零。

---

## 4. `AIRE_ACON` 变量口径

### 4.1 变量基本信息

| 项目 | 内容 |
| --- | --- |
| 表 | `Viviendas_CA` |
| 变量名 | `AIRE_ACON` |
| 标签 | `Equipamiento (aire acondicionado)` |
| 问题文本 | `¿En esta vivienda tienen: aire acondicionado?` |
| 变量类型 | 离散型 / numeric |

### 4.2 INEGI 定义

INEGI 元数据对该变量的定义是：

```text
Distinción de las viviendas particulares habitadas según cuenten con un aparato que permita enfriar los ambientes cerrados.
```

访员说明中对 air conditioner 的解释为：

```text
El aire acondicionado es un aparato que se usa para enfriar los ambientes cerrados, por ejemplo, cuartos de la vivienda. Son frecuentes en lugares muy calurosos.
```

### 4.3 宇宙

`AIRE_ACON` 的宇宙为：

```text
Viviendas particulares habitadas, excepto locales no construidos para habitación, viviendas móviles y refugios.
```

中文解释：

该变量统计的是有人居住的私人住房，不包括非居住用途场所、移动住房和避难所。

### 4.4 编码

`AIRE_ACON` 的编码如下：

| 编码 | 含义 | 是否用于主判断 |
| --- | --- | --- |
| `5` | Sí / 有空调 | 是 |
| `6` | No / 无空调 | 是 |
| `9` | No especificado / 未说明 | 单独记录，不并入“无空调” |

重要注意：

- 不能把 `AIRE_ACON` 当成 1/0 变量。
- `9` 不是“无空调”，而是未说明。
- `AIRE_ACON` 只表示“住房是否拥有空调设备”，不表示台数、容量、品牌、能效、购买年份或使用频率。

---

## 5. `FACTOR` 权重口径

### 5.1 变量基本信息

| 项目 | 内容 |
| --- | --- |
| 表 | `Viviendas_CA` |
| 变量名 | `FACTOR` |
| 标签 | `Factor de Expansión` |
| 概念 | `Factor`、`Ponderador` |

### 5.2 INEGI 定义

INEGI 元数据对 `FACTOR` 的定义是：

```text
Cantidad de viviendas en el universo, que representa una vivienda en la muestra.
```

中文解释：

`FACTOR` 表示样本中的一条住房记录代表总体中的多少住房。

### 5.3 使用规则

后续所有州级拥有率计算必须使用 `FACTOR` 加权。

不采用未加权样本比例作为主结果，原因是：

- `Viviendas_CA` 来自扩展问卷/样本数据。
- 样本记录数不是总体住房数。
- `FACTOR` 才是将样本记录扩展到总体住房的权重。

---

## 6. 计算公式

### 6.1 基础加权量

对任一地区 `g`，定义：

```text
加权住房总数 = sum(FACTOR)
加权有空调住房数 = sum(FACTOR where AIRE_ACON = 5)
加权无空调住房数 = sum(FACTOR where AIRE_ACON = 6)
加权未说明住房数 = sum(FACTOR where AIRE_ACON = 9)
```

### 6.2 主口径：总分母拥有率

后续主表默认使用：

```text
空调拥有率_总分母 = 加权有空调住房数 / 加权住房总数
```

选择该口径的原因：

- 与“有空调住房占全部有人居住私人住房”的市场渗透含义更一致。
- 对 `No especificado` 保持保守处理，不把它错误归为“无空调”。

### 6.3 敏感口径：有效分母拥有率

同时保留：

```text
空调拥有率_有效分母 = 加权有空调住房数 / (加权有空调住房数 + 加权无空调住房数)
```

使用该口径的目的：

- 检查 `No especificado` 对结果的影响。
- 如果某州 `No especificado` 占比异常，需要在 03B 中提示。

### 6.4 未说明占比

```text
未说明占比 = 加权未说明住房数 / 加权住房总数
```

如果该比例较高，需要谨慎解释该州拥有率。

---

## 7. 字段清单

后续 03B 至少保留以下字段：

| 字段 | 来源字段/计算 | 说明 |
| --- | --- | --- |
| `ent_code` | `ENT` | 州代码，字符串 |
| `state_name` | 外部映射 | 州名 |
| `sample_n` | 记录数 | 样本住房记录数，不是总体住房数 |
| `weighted_total` | `sum(FACTOR)` | 加权住房总数 |
| `weighted_ac_yes` | `sum(FACTOR where AIRE_ACON=5)` | 加权有空调住房数 |
| `weighted_ac_no` | `sum(FACTOR where AIRE_ACON=6)` | 加权无空调住房数 |
| `weighted_unspecified` | `sum(FACTOR where AIRE_ACON=9)` | 加权未说明住房数 |
| `ac_rate_total_denominator` | `weighted_ac_yes / weighted_total` | 主口径 |
| `ac_rate_valid_denominator` | `weighted_ac_yes / (weighted_ac_yes + weighted_ac_no)` | 敏感口径 |
| `unspecified_rate` | `weighted_unspecified / weighted_total` | 数据质量提示 |
| `source_url` | INEGI URL | 州级 ZIP 下载链接 |

---

## 8. 微数据下载路径

已确认 INEGI 州级 CSV ZIP 路径可用，路径模式为：

```text
https://www.inegi.org.mx/contenidos/programas/ccpv/2020/microdatos/Censo2020_CA_{STATE_ABBR}_csv.ZIP
```

03B 第一阶段重点热区州使用以下路径：

| 州 | 缩写 | URL 验证状态 |
| --- | --- | --- |
| Baja California | `BC` | 已验证 ZIP 响应 |
| Baja California Sur | `BCS` | 已验证 ZIP 响应 |
| Sonora | `SON` | 已验证 ZIP 响应 |
| Sinaloa | `SIN` | 已验证 ZIP 响应 |
| Nuevo León | `NL` | 已验证 ZIP 响应 |
| Tamaulipas | `TAM` | 已验证 ZIP 响应 |
| Tabasco | `TAB` | 已验证 ZIP 响应 |
| Quintana Roo | `QROO` | 已验证 ZIP 响应 |

验证方式：

- 使用 HTTP range 读取文件头。
- ZIP 文件头魔数为 `504b0304`。
- 该验证只说明下载路径指向 ZIP 文件，不等于已完成数据计算。

示例 URL：

```text
https://www.inegi.org.mx/contenidos/programas/ccpv/2020/microdatos/Censo2020_CA_SON_csv.ZIP
```

---

## 9. 实际表头核验

已使用已下载的 `Aguascalientes` 州 ZIP 抽样核验 `Viviendas01.CSV` 表头。表头包含以下关键字段：

```text
ENT,MUN,LOC50K,ID_VIV,COBERTURA,ESTRATO,UPM,FACTOR,...,AIRE_ACON,...,TAMLOC
```

这说明后续 03B 至少可以从 CSV 中直接读取：

- `ENT`
- `MUN`
- `LOC50K`
- `ID_VIV`
- `FACTOR`
- `AIRE_ACON`
- `TAMLOC`

注意：

- 该表头核验只验证字段存在。
- 不代表所有州已经正式完成下载和计算。
- 正式计算时每个州仍需单独下载、解压、读取、计算并记录来源。

---

## 10. 03B 的执行边界

03B 只做：

1. 下载 8 个重点热区州的 `ViviendasXX.CSV`。
2. 读取 `FACTOR` 与 `AIRE_ACON`。
3. 按本文公式计算加权住房数和空调拥有率。
4. 输出州级结果表。
5. 标注数据限制。

03B 不做：

- 不做全 32 州，除非 8 州试算通过。
- 不做 municipio 级。
- 不做气候、收入、电价合成指数。
- 不推断品牌、容量段、inverter 占比、用户使用频率。
- 不把空调拥有率解释为销量或市场份额。

---

## 11. 数据限制

| 限制 | 解释 |
| --- | --- |
| 时间点限制 | 数据来自 2020 年 Censo，不能直接代表 2026 年现状 |
| 指标含义限制 | `AIRE_ACON` 是拥有情况，不是销量、使用量或台数 |
| 样本限制 | `Viviendas_CA` 是扩展问卷数据，需使用 `FACTOR` |
| 缺失处理限制 | `No especificado` 需要单独记录 |
| 地理解释限制 | 州级结果不能替代城市/municipio 差异 |
| 精度限制 | 本阶段不计算标准误、置信区间或变异系数 |

---

## 12. 已确认与待确认事项

### 已确认

| 项目 | 状态 |
| --- | --- |
| `AIRE_ACON` 所在表 | 已确认在 `Viviendas_CA` |
| `AIRE_ACON` 编码 | 已确认：`5=Sí`、`6=No`、`9=No especificado` |
| `AIRE_ACON` 实际空白值 | 03B 执行时发现 8 个重点州微数据中存在空白值；官方元数据未将空白列为分类，后续统一单独统计 |
| `AIRE_ACON` 宇宙 | 已确认：有人居住私人住房，排除非住房场所、移动住房和避难所 |
| `FACTOR` 定义 | 已确认：样本住房代表的总体住房数量 |
| `Viviendas_CA` 关键字段 | 已确认包含 `ENT`、`MUN`、`FACTOR`、`AIRE_ACON` |
| 8 个重点热区州 ZIP 路径 | 已验证为 ZIP 文件 |

### 待确认

| 项目 | 后续处理 |
| --- | --- |
| 8 个重点州正式计算结果 | 已进入 03B，产出 `03B_20260522-152622_墨西哥州级空调拥有率_INEGI2020.md` |
| 32 州完整结果 | 03B 试算通过后再扩展 |
| municipio 级结果 | 第二阶段，需额外处理精度和计算量 |
| 标准误/置信区间 | 后续如需要，再查 INEGI 精度资料或自行按复杂抽样设计估算 |
| 与收入、电价、热负荷的合成 | 进入 03G，不在 03A 做 |

---

## 13. 对后续子任务的调整建议

基于 03A，后续子任务建议如下：

1. 03B 先跑 8 个重点热区州，不直接跑 32 州。
2. 03B 输出中必须同时给出总分母口径和有效分母口径。
3. 03B 中必须列出 `No especificado` 占比，避免缺失值影响判断。
4. 03B 执行中发现实际微数据存在 `AIRE_ACON` 空白值；后续所有拥有率任务必须单独列出空白值，不得并入“无空调”或官方编码 `9`。
5. municipio 级拥有率暂不并入 03B 第一版，避免任务过重。
6. 03C、03D、03E 暂不调整；等待 03B 结果后再决定是否优先补 CFE、电价还是气候。
