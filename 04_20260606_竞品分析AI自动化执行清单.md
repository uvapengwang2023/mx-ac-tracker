# 竞品分析 AI 自动化执行清单

目标：把墨西哥空调竞品分析拆成可逐一闭环的自动化模块。每个模块都要明确数据输入、AI/脚本动作、输出物、验收标准和人工关口。

## 闭环顺序

建议顺序是先把数据底座跑稳，再做规格标准化，然后才做竞品分层和策略判断：

1. 多平台商品与价格每日更新
2. SKU 标准字段自动抽取
3. 竞品池自动分层
4. 价格带与机会区间自动识别
5. Listing 内容诊断
6. 价格变化预警
7. 评论与口碑主题挖掘
8. 月度竞品简报自动生成

## 2026-06-06 规划更新

当前阶段已经从“规划自动化模块”推进到“云端 MVP + SKU 标准字段第一轮复核”。

阶段判断：

- A01 已经基本闭环：GitHub Actions 三站点云端任务已跑通，artifact 可以下载，dashboard 数据可以查看；下一步是连续观察 2-3 天定时任务。
- A02 已完成第一轮：SKU 标准字段脚本、SQLite 表、dashboard 模块、30 样本复核已经完成；下一步不是继续盲目加规则，而是建立重点品牌系列词典。
- A03 已具备启动条件：可以用 `brandNorm + productSeries/modelFamily + capacityTon + voltageClass + inverter + coolingMode + productType + price` 生成候选竞品池。

新增口径：

- 产品系列不能只看标题显式词。
- 型号和系列名之间通常有映射关系，型号前缀/后缀/字母段可以作为推断系列的重要线索。
- 标题明确系列名给高置信度；型号特征推断出的系列保留为低置信度 `model_family`，必须进入人工复核。
- 功能词/营销词不能直接当系列，例如 `WiFi Ready`、`3D Air Flow`、`Smart`。

## 执行清单

| ID | 优先级 | 模块 | AI/脚本可做 | 你必须决定 | 验收标准 | 当前状态 |
|---|---|---|---|---|---|---|
| A01 | P0 | 多平台商品与价格每日更新 | 串联 crawler，本机可用 LaunchAgent，云端可用 GitHub Actions + PROXY_URL Secret，运行前检查代理/站点可达性 | 运行时间、云端代理、验证码处理、必须覆盖的平台、数据持久化方式 | 云端 workflow 可定时触发；失败平台显示原因；至少两次抓取后能识别变价 | 云端 MVP 已验证，待连续观察 |
| A02 | P0 | SKU 标准字段自动抽取 | 从标题、型号和详情页抽取品牌、产品系列、型号、吨位/BTU、电压、变频、冷暖、安装类型；区分标题显式系列和型号特征推断系列 | 标准字段口径，型号到系列的映射规则，关键 SKU 抽样复核，尤其确认系列名 | 核心字段覆盖率超过 85%；低置信度 SKU 单独列出；抽样准确率可接受；型号推断保留来源和置信度 | 第一轮完成，进入系列词典 |
| A03 | P0 | 竞品池自动分层 | 按价格带、规格相似度、品牌和平台覆盖生成候选竞品分组 | 最终确认哪些是真竞品，哪些只是噪音或不可比 | 每个目标 SKU 至少给出 5-10 个候选可比竞品；排除明显非同类 | 准备启动 |
| A04 | P1 | 价格带与机会区间自动识别 | 计算价格带、中位价、分位数、折扣深度、异常高低价 | 成本、毛利底线、是否跟价或差异化 | 能按规格输出价格区间；异常项可点回商品链接 | 已打底 |
| A05 | P1 | Listing 内容诊断 | 评估标题、图片、卖点、规格完整度和本地化表达 | 品牌语气、合规措辞、可真实承诺的卖点 | 每个重点竞品有一页诊断；能指出标题、规格、图文、服务承诺缺口 | 待补详情页 |
| A06 | P1 | 价格变化预警 | 按阈值生成降价、涨价、新品、下架提醒 | 重点品牌/SKU 清单和触发阈值 | 每条提醒有原因、幅度、链接和建议动作 | 已打底 |
| A07 | P2 | 评论与口碑主题挖掘 | 评论聚类、情绪分类、痛点趋势和典型摘录 | 判断哪些痛点是结构性机会 | 每个重点品牌输出 Top 痛点和正向卖点；低样本商品标注不足 | 待数据源 |
| A08 | P2 | 月度竞品简报自动生成 | 生成月度价格、上新、品牌变化、机会点和关注项 | 审定最终结论和策略建议 | 能回答本月谁降价、谁上新、哪个价格带变热、下月看什么 | 待前置项 |

## 当前阶段建议：A02 收口并启动 A03

原因：现在数据底座和 SKU 标准字段已经有第一轮结果，可以开始从“字段抽取”进入“可比竞品候选”。但在 A03 正式分层前，要先把重点品牌的系列词典固定下来，避免把低置信度型号族误当成最终产品线。

A02 已完成范围：

- 输入：现有 `data/*/latest_products.json`
- 输出：`data/normalized_skus/latest_normalized_skus.json`、`data/normalized_skus/latest_normalized_skus.csv`、SQLite `sku_normalized_fields` 表
- 字段：brand、product_series、model_code、category、capacity_ton、btu、voltage、inverter、cooling_mode、installation_type、confidence、review_needed_reason
- 验收：已完成 30 个重点 SKU 复核，覆盖 Prime、Mirage、Midea、Mabe、LG

A02 剩余收口：

- 建立重点品牌系列词典。
- 把标题显式系列、型号特征推断系列、功能词/营销词分开管理。
- 对 Prime `B -> Bright family`、Midea `MAS/MAW/MAP`、Mabe `MMI/MMT/PTM`、LG `MT/W/VX/VM` 等型号族做人工确认。
- 把确认后的词典写入脚本或独立 JSON，供 A03 竞品分层使用。

A03 启动口径：

- 先以 Prime 自有 SKU 为目标池。
- 每个目标 SKU 生成 5-10 个候选竞品。
- 候选匹配字段优先级：产品类型、容量、变频/定频、单冷/冷暖、电压、系列/型号族、价格带、品牌、平台。
- 输出必须保留可比原因和不确定原因，让你最终判断“真竞品/弱竞品/不可比”。

## A01 定时运行落地方式

现在已经新增两个脚本：

- `npm run daily:crawl`：每日任务包装器，负责检查网络/VPN、依次跑 crawler、生成日志、刷新 dashboard 数据。
- `npm run daily:install`：把每日任务安装成 macOS LaunchAgent。

建议先跑 dry run 验证：

```bash
npm run daily:crawl -- --dry-run --skip-preflight
```

安装每天 9:00 自动运行：

```bash
npm run daily:install -- --hour=9 --minute=0
```

只跑部分平台：

```bash
npm run daily:install -- --hour=9 --minute=0 --sites=coppel,elektra,homedepotmx
```

卸载定时任务：

```bash
npm run daily:uninstall
```

VPN 口径：

- 如果 VPN 客户端不能被命令行启动，就需要你在定时运行前保持 VPN 已连接。
- 可以在 `.env.crawler` 里设置 `VPN_CHECK_URL=https://www.homedepot.com.mx/`，让每日任务先检查这个地址。
- WalmartMX 目前仍需要人工验证，默认不会放进无人值守定时任务；要跑 Walmart，继续用手动脚本更稳。

## A01 云端运行落地方式

已经新增 GitHub Actions 工作流：

- `.github/workflows/cloud-daily-crawl.yml`

云端运行前需要准备：

- 把项目推到 GitHub 仓库。
- 在仓库 Secrets 添加 `PROXY_URL`。
- 可选添加 `VPN_CHECK_URL=https://www.homedepot.com.mx/`。
- 在 Actions 里启用 `Cloud Daily Crawl`。

云端默认每天运行：

- Coppel
- Elektra
- HomeDepotMX

云端不默认跑 WalmartMX，因为当前 WalmartMX 仍需要人工验证。

云端产物：

- `data/daily_crawls/` 日志
- 各平台 latest JSON
- `data/normalized_skus/` SKU 标准字段
- `data/daily_alerts/` 每日提醒
- `data/ac_price_monitor_mexico_v2.db` SQLite 数据库
- `web/competitor-intel-dashboard/data.js`
- GitHub Actions artifact
- 可选 GitHub Pages 发布
